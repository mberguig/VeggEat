package com.app.berguig.veggeat;
/*
import android.app.Fragment;
import android.os.Bundle;

import java.util.UUID;
import com.app.berguig.veggeat.VeggEatLab;

/**
 * Created by stagiaire on 16/06/2017.
 */
/*
public class RestaurantFragment extends Fragment{
Restaurant mRestaurant;

    //@Override
    public void onCreate(Bundle savedInstanceState){
        super.onCreate(savedInstanceState);
        UUID restaurantId = (UUID) getArguments().getSerializable(ARG_RESTAURANT_ID);
        mRestaurant = VeggEatLab.get(getActivity()).getRestaurant(restaurantId);
    }

    //push l'update
    //@Override
    public void onPause(){
        super.onPause();
        VeggEatLab.get(getActivity()).updateRestaurant(mRestaurant);
    }
}
*/